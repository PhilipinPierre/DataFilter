<UserControl x:Class="DataFilter.Wpf.Demo.Views.CollectionViewFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:DataFilter.Wpf.Controls;assembly=DataFilter.Wpf.PopupHost"
             xmlns:viewmodels="clr-namespace:DataFilter.Wpf.Demo.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type={x:Type viewmodels:CollectionViewScenarioViewModel}, IsDesignTimeCreatable=True}">
    <Grid>
        <!--<ListView ItemsSource="{Binding CollectionView}"
                  VirtualizingPanel.IsVirtualizing="True">
            <ListView.View>
                <controls:FilterableGridView ViewModel="{Binding GridViewModel}">
                    <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Id}" Width="50"/>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                    <GridViewColumn Header="Department" DisplayMemberBinding="{Binding Department}" Width="150"/>
                    <GridViewColumn Header="Country" DisplayMemberBinding="{Binding Country}" Width="150"/>
                    <GridViewColumn Header="Time" DisplayMemberBinding="{Binding Time}" Width="150"/>
                </controls:FilterableGridView>
            </ListView.View>
        </ListView>-->

        <controls:FilterableDataGrid Grid.Column="1"
                                     ViewModel="{Binding GridViewModel}"
                                     ItemsSource="{Binding GridViewModel.FilteredItems}"
                                     AutoGenerateColumns="True"
                                     VirtualizingPanel.IsVirtualizing="True"
                                     FilterContext="{Binding GridViewModel.Context}" />
        
    </Grid>
</UserControl>
