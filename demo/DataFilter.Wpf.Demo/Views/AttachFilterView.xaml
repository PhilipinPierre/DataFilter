<UserControl x:Class="DataFilter.Wpf.Demo.Views.AttachFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:DataFilter.Wpf.Demo.ViewModels"
             xmlns:behaviors="clr-namespace:DataFilter.Wpf.Behaviors;assembly=DataFilter.Wpf"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type={x:Type viewmodels:LocalFilterScenarioViewModel}, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Attach demo — WPF DataGrid (no control replacement)"
                       FontSize="20"
                       FontWeight="Bold" />
            <TextBlock Text="This uses attached properties to enable filtering on a standard DataGrid."
                       FontSize="14"
                       Foreground="Gray" />
        </StackPanel>

        <DataGrid Grid.Row="1"
                  Margin="10,0,10,10"
                  AutoGenerateColumns="True"
                  ItemsSource="{Binding GridViewModel.FilteredItems}"
                  behaviors:FilterableGridAttach.Attach="True"
                  behaviors:FilterableGridAttach.ViewModel="{Binding GridViewModel}" />
    </Grid>
</UserControl>

