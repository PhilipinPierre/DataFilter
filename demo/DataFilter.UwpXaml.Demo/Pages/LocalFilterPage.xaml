<Page
    x:Class="DataFilter.UwpXaml.Demo.Pages.LocalFilterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:DataFilter.UwpXaml.Controls"
    mc:Ignorable="d">

    <Grid>
        <controls:FilterableDataGrid 
            ViewModel="{x:Bind ViewModel.GridViewModel, Mode=OneWay}"
            ItemsSource="{x:Bind ViewModel.GridViewModel.FilteredItems, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="{Binding Id}" Width="50" />
                        <TextBlock Text="{Binding Name}" Width="150" />
                        <TextBlock Text="{Binding Department}" Width="150" />
                        <TextBlock Text="{Binding Country}" Width="150" />
                        <TextBlock Text="{Binding Time}" Width="100" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </controls:FilterableDataGrid>
    </Grid>
</Page>
