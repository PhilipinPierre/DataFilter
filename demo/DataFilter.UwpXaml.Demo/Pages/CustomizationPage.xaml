<Page
    x:Class="DataFilter.UwpXaml.Demo.Pages.CustomizationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:DataFilter.UwpXaml.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="0" Margin="10,20,10,10" Spacing="15">
            <TextBlock Text="Scenario 4 — UI Customization" FontSize="20" FontWeight="Bold" TextWrapping="Wrap" />
            <TextBlock Text="Change the toggle to customize colors dynamically. This demonstrates how the filtering UI can be themed to match your application's appearance." 
                       TextWrapping="Wrap" FontSize="14" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
            
            <ToggleSwitch Header="Dark Theme" 
                          IsOn="{x:Bind ViewModel.IsDarkTheme, Mode=TwoWay}"
                          Toggled="ToggleSwitch_Toggled"/>
            
            <TextBlock Text="In UWP, requested theme changes dynamically update the controls." 
                       TextWrapping="Wrap" FontSize="12" Foreground="Gray" />
        </StackPanel>

        <controls:FilterableDataGrid 
            Grid.Column="1"
            ViewModel="{x:Bind ViewModel.GridViewModel, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="{Binding Id}" Width="50" />
                        <TextBlock Text="{Binding Name}" Width="150" />
                        <TextBlock Text="{Binding Department}" Width="150" />
                        <TextBlock Text="{Binding Country}" Width="150" />
                        <TextBlock Text="{Binding Time}" Width="100" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </controls:FilterableDataGrid>
    </Grid>
</Page>
