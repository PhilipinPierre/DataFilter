<Page
    x:Class="DataFilter.UwpXaml.Demo.Pages.ListViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:DataFilter.UwpXaml.Controls"
    mc:Ignorable="d">

    <Grid Opacity="{x:Bind Opacity, Mode=OneWay}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10" Spacing="5">
            <TextBlock Text="Scenario 5 — Native ListView Integration" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="Demonstrating DataFilter integration with a standard UWP ListView control using manual filter triggers." FontSize="14" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" />
        </StackPanel>

        <!-- Manual Header with Filter Buttons -->
        <Border Grid.Row="1" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}" Padding="10,5">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <Button Content="Filter ID" Click="FilterButton_Click" Tag="Id" />
                <Button Content="Filter Name" Click="FilterButton_Click" Tag="Name" />
                <Button Content="Filter Dept" Click="FilterButton_Click" Tag="Department" />
                <Button Content="Filter Country" Click="FilterButton_Click" Tag="Country" />
            </StackPanel>
        </Border>

        <ListView x:Name="DataListView" Grid.Row="2"
                  SelectionMode="None">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="ID" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="Name" FontWeight="Bold"/>
                        <TextBlock Grid.Column="2" Text="Dept" FontWeight="Bold"/>
                        <TextBlock Grid.Column="3" Text="Country" FontWeight="Bold"/>
                        <TextBlock Grid.Column="4" Text="Time" FontWeight="Bold"/>
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Id}" />
                        <TextBlock Grid.Column="1" Text="{Binding Name}" />
                        <TextBlock Grid.Column="2" Text="{Binding Department}" />
                        <TextBlock Grid.Column="3" Text="{Binding Country}" />
                        <TextBlock Grid.Column="4" Text="{Binding Time}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
