<Page
    x:Class="DataFilter.WinUI3.Demo.Pages.LocalFilterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:DataFilter.WinUI3.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10" Spacing="5">
            <TextBlock Text="Scenario 1 — Local Filtering" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="All data is loaded locally. Filters are applied in-memory instantly against the collection." FontSize="14" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" />
        </StackPanel>

        <controls:FilterableDataGrid Grid.Row="1"
            ViewModel="{x:Bind ViewModel.GridViewModel, Mode=OneWay}"
            ItemsSource="{x:Bind ViewModel.GridViewModel.FilteredItems, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="{Binding Id}" Width="50" />
                        <TextBlock Text="{Binding Name}" Width="150" />
                        <TextBlock Text="{Binding Department}" Width="150" />
                        <TextBlock Text="{Binding Country}" Width="150" />
                        <TextBlock Text="{Binding Time}" Width="100" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </controls:FilterableDataGrid>
    </Grid>
</Page>
