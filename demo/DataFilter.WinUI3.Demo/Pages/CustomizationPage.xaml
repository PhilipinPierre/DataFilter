<Page
    x:Class="DataFilter.WinUI3.Demo.Pages.CustomizationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:DataFilter.WinUI3.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="0" Margin="10,0,10,0" Spacing="10">
            <TextBlock Text="Customization" FontWeight="Bold" FontSize="16" />
            <ToggleSwitch Header="Dark Theme" 
                          IsOn="{x:Bind ViewModel.IsDarkTheme, Mode=TwoWay}"
                          Toggled="ToggleSwitch_Toggled"/>
            <TextBlock Text="In WinUI3, requested theme changes dynamically update the controls." 
                       TextWrapping="Wrap" Foreground="Gray" Margin="0,10,0,0"/>
        </StackPanel>

        <controls:FilterableDataGrid 
            Grid.Column="1"
            ViewModel="{x:Bind ViewModel.GridViewModel, Mode=OneWay}"
            ItemsSource="{x:Bind ViewModel.GridViewModel.FilteredItems, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="{Binding Id}" Width="50" />
                        <TextBlock Text="{Binding Name}" Width="150" />
                        <TextBlock Text="{Binding Department}" Width="150" />
                        <TextBlock Text="{Binding Country}" Width="150" />
                        <TextBlock Text="{Binding Time}" Width="100" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </controls:FilterableDataGrid>
    </Grid>
</Page>
