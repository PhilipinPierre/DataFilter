# DataFilter.Maui.Demo

MAUI demo application for the DataFilter library.

## Runtime language switching (localization test)

This demo includes a **Language** picker in the top title bar (Shell `TitleView`) to switch the UI culture at runtime.

The available languages come from the satellite resource assemblies embedded in **`DataFilter.Localization`**.

