<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:DataFilter.Wpf.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/DataFilter.Wpf;component/Themes/FilterLightTheme.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Default Template for ColumnFilterButton -->
    <Style TargetType="{x:Type controls:ColumnFilterButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="HorizontalAlignment"
                Value="Right" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ColumnFilterButton}">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentTemplate="{TemplateBinding IconTemplate}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#1A000000" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="IconTemplate">
            <Setter.Value>
                <DataTemplate>
                    <!-- Default Funnel Icon -->
                    <Path Data="M 2 2 L 14 2 L 10 7 L 10 13 L 6 15 L 6 7 Z" 
                          Fill="{Binding RelativeSource={RelativeSource AncestorType=controls:ColumnFilterButton}, Path=InactiveBrush}" 
                          Stretch="Fill" 
                          Width="12" Height="14" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsActive" Value="True">
                <Setter Property="IconTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <!-- Colored Funnel Icon -->
                            <Path Data="M 2 2 L 14 2 L 10 7 L 10 13 L 6 15 L 6 7 Z" 
                                  Fill="{Binding RelativeSource={RelativeSource AncestorType=controls:ColumnFilterButton}, Path=ActiveBrush}" 
                                  Stretch="Fill" 
                                  Width="12" Height="14" />
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
