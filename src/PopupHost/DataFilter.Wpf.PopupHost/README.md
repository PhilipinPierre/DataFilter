# DataFilter.Wpf.PopupHost

Hosts and positions the WPF filter popup (open/close lifecycle, LTR/RTL anchoring, window clamping).

This package contains only the popup hosting logic. UI controls remain in `DataFilter.Wpf`.

